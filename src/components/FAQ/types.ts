export interface FAQ {
    question: string;
    answer: string;
    id: string;
  }
  export type NumberingStyle = 'numeric' | 'alphabetic' | 'roman' | 'custom';
  export type AnswerStyle = 'bullet' | 'arrow' | 'custom' | 'dash' | 'none';